import { createAction } from 'redux-actions';

export const loadItemsAction = createAction('LOAD_ITEMS');
export const deleteItemAction = createAction('DELETE_ITEM');
export const sortByAlbumIdAction = createAction('SORT_BY_ALBBUMID');
